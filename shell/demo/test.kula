str := "hello_world";
arr := asArray();

for (i := 0; i < str.length(); i = i + 1) {
    if (str.at(i) == "_") {
        break;
    }
    print str.at(i);
    arr.insert(arr.length(), str.at(i));
}

print arr;
